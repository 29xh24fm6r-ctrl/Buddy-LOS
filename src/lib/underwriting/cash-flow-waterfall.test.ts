import{describe,expect,it}from"vitest";import{computeCashFlowWaterfall,type CashFlowWaterfallInput}from"./cash-flow-waterfall";
const input:CashFlowWaterfallInput={netIncomeBase:100000,depreciation:20000,amortization:5000,section179Normalized:10000,bonusDepreciationNormalized:null,interestExpense:15000,nonRecurringIncome:5000,nonRecurringExpense:12000,ownerBenefitAddbacks:[8000,2000],normalizedTaxProvision:null,maintenanceCapex:10000,annualDebtService:100000,isPassThrough:true};
describe("Buddy cash flow waterfall source parity",()=>{it("computes reported adjusted owner-adjusted EBITDA and NCADS in canonical order",()=>{const r=computeCashFlowWaterfall(input);expect(r.reportedEbitda).toBe(150000);expect(r.qualityOfEarningsAdjustment).toBe(7000);expect(r.adjustedEbitda).toBe(157000);expect(r.ownerAdjustedEbitda).toBe(167000);expect(r.ncads).toBe(157000);expect(r.dscr).toBe(1.57);});it("does not fabricate a ratio with missing income or nonpositive debt service",()=>{expect(computeCashFlowWaterfall({...input,netIncomeBase:null,depreciation:null,amortization:null,section179Normalized:null,interestExpense:null}).ncads).toBeNull();expect(computeCashFlowWaterfall({...input,annualDebtService:0}).dscr).toBeNull();});it("retains formula lineage for every decision metric",()=>{expect(computeCashFlowWaterfall(input).lineage.find(x=>x.key==="DSCR")?.inputs).toEqual(["CF_NCADS","ANNUAL_DEBT_SERVICE"]);});});
