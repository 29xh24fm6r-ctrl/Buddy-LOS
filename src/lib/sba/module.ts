export const SBA_RULESET_VERSION="buddy-sba-7a.v1" as const;
export type SbaInput={forProfit:boolean|null;operatesInUs:boolean|null;ineligibleActivity:boolean|null;delinquentFederalDebt:boolean|null;ownersEligible:boolean|null;loanAmount:number;isAcquisition:boolean;realEstateUse:boolean;singleOwner:boolean;fullySecured:boolean};
export function evaluateSbaEligibility(input:SbaInput){const missing=(["forProfit","operatesInUs","ineligibleActivity","delinquentFederalDebt","ownersEligible"]as const).filter(k=>input[k]===null);const blockers:string[]=[];if(input.forProfit===false)blockers.push("NOT_FOR_PROFIT");if(input.operatesInUs===false)blockers.push("NON_US_OPERATIONS");if(input.ineligibleActivity===true)blockers.push("INELIGIBLE_ACTIVITY");if(input.delinquentFederalDebt===true)blockers.push("DELINQUENT_FEDERAL_DEBT");if(input.ownersEligible===false)blockers.push("OWNERSHIP_INELIGIBLE");return{rulesetVersion:SBA_RULESET_VERSION,status:blockers.length?"INELIGIBLE":missing.length?"UNKNOWN":"ELIGIBLE",blockers,missing,advisoryOnly:true as const};}
export function requiredSbaArtifacts(input:SbaInput,taxYears:number[]){const artifacts=taxYears.flatMap(year=>[`BUSINESS_TAX_RETURN_${year}`,`PERSONAL_TAX_RETURN_${year}`]);artifacts.push("PERSONAL_FINANCIAL_STATEMENT","YTD_FINANCIAL_STATEMENTS","BUSINESS_DEBT_SCHEDULE","SBA_1919");if(input.realEstateUse)artifacts.push("REAL_ESTATE_APPRAISAL","TITLE_COMMITMENT");if(input.isAcquisition)artifacts.push("BUSINESS_VALUATION");if(input.loanAmount>50_000)artifacts.push("HAZARD_INSURANCE");if(input.loanAmount>350_000&&input.singleOwner&&!input.fullySecured)artifacts.push("LIFE_INSURANCE");artifacts.push("UCC_LIEN_SEARCH");return[...new Set(artifacts)];}
