import{describe,expect,it}from"vitest";import{evaluateSbaEligibility,requiredSbaArtifacts}from"./module";const base={forProfit:true,operatesInUs:true,ineligibleActivity:false,delinquentFederalDebt:false,ownersEligible:true,loanAmount:400000,isAcquisition:true,realEstateUse:true,singleOwner:true,fullySecured:false};describe("separable SBA module",()=>{it("fails closed on missing facts",()=>expect(evaluateSbaEligibility({...base,ownersEligible:null})).toMatchObject({status:"UNKNOWN",advisoryOnly:true,missing:["ownersEligible"]}));it("identifies deterministic blockers",()=>expect(evaluateSbaEligibility({...base,delinquentFederalDebt:true})).toMatchObject({status:"INELIGIBLE",blockers:["DELINQUENT_FEDERAL_DEBT"]}));it("builds the governed document and third-party checklist",()=>{const items=requiredSbaArtifacts(base,[2025,2024]);expect(items).toContain("SBA_1919");expect(items).toContain("BUSINESS_VALUATION");expect(items).toContain("LIFE_INSURANCE");expect(items).toContain("UCC_LIEN_SEARCH")})});
