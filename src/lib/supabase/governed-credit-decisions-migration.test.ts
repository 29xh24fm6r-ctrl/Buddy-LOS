import{readFileSync}from"node:fs";import{resolve}from"node:path";import{describe,expect,it}from"vitest";const sql=readFileSync(resolve("supabase/migrations/20260812230000_governed_credit_decisions.sql"),"utf8").toLowerCase();describe("governed credit decisions migration",()=>{it("requires certification and named condition owners",()=>{expect(sql).toContain("certification_statement");expect(sql).toContain("owner_user_id uuid not null")});it("constrains approval authority",()=>expect(sql).toContain("check(approver_role in('owner','administrator','underwriter'))"));it("makes final decisions immutable",()=>{expect(sql).toContain("credit decisions are immutable");expect(sql).toContain("before update or delete")});it("does not grant ordinary write or autonomous approval",()=>{expect(sql).not.toMatch(/grant (insert|update|delete|all).*authenticated/);expect(sql).toContain("no autonomous approval path")})});
