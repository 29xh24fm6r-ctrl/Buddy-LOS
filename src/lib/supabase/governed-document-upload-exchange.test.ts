import{readFileSync}from'node:fs';import{resolve}from'node:path';import{describe,expect,it}from'vitest';const sql=readFileSync(resolve('supabase/migrations/20260811223612_governed_document_upload_exchange.sql'),'utf8').toLowerCase();describe('governed upload exchange',()=>{it('closes ambiguous replay',()=>{expect(sql).toContain('document upload idempotency conflict');expect(sql).toContain("v_event.actor_user_id <> v_actor");});it('requires user authority before finalize',()=>{expect(sql).toContain('authorize_pending_document_finalize');expect(sql).toContain("m.role in ('owner','administrator')");expect(sql).toContain('a.ended_at is null');});it('moves verified bytes only to quarantine',()=>{expect(sql).toContain("security_status='quarantined',sha256=p_sha256");expect(sql).toContain("'document_upload.persisted'");});it('keeps storage policies absent',()=>expect(sql).not.toMatch(/create policy[^;]+on storage\.objects/));});
