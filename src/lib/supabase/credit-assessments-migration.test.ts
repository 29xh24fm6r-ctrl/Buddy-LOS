import{readFileSync}from"node:fs";import{resolve}from"node:path";import{describe,expect,it}from"vitest";const migration=readFileSync(resolve("supabase/migrations/20260812220000_credit_assessments.sql"),"utf8").toLowerCase();describe("credit assessments migration",()=>{it("binds assessment to certified-input lineage",()=>{expect(migration).toContain("foreign key(spread_id,organization_id)");expect(migration).toContain("engine_version='buddy-credit-engine.v1'");expect(migration).toContain("policy_version text not null");});it("records exceptions separately",()=>{expect(migration).toContain("create table public.credit_policy_exceptions");expect(migration).toContain("resolution_rationale text");});it("does not expose writes or decision authority",()=>{expect(migration).not.toMatch(/grant\s+(insert|update|delete|all)/);expect(migration).not.toContain("create function public.approve_credit");});});
