import{readFileSync}from"node:fs";import{resolve}from"node:path";import{describe,expect,it}from"vitest";const migration=readFileSync(resolve("supabase/migrations/20260812210000_underwriting_runtime.sql"),"utf8").toLowerCase();
describe("underwriting runtime migration",()=>{it("leases with recovery and skip locked",()=>{expect(migration).toContain("create function public.claim_underwriting_outbox_event");expect(migration).toContain("for update skip locked");expect(migration).toContain("worker lease expired");});it("revalidates clean immutable documents",()=>{expect(migration).toContain("document.security_status='clean'");expect(migration).toContain("document.sha256=snapshot.sha256");expect(migration).toContain("document.deleted_at is null");});it("persists only matching versioned evidence",()=>{expect(migration).toContain("document intelligence authority mismatch");expect(migration).toContain("document intelligence evidence mismatch");expect(migration).toContain("record_document_intelligence_artifact");});it("keeps worker functions service-role only",()=>{expect(migration.match(/grant execute on function/g)?.length).toBe(4);expect(migration).not.toMatch(/grant execute on function .* to authenticated/);});});
