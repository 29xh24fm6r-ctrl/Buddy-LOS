import{readFileSync}from"node:fs";import{resolve}from"node:path";import{describe,expect,it}from"vitest";const sql=readFileSync(resolve("supabase/migrations/20260813000000_underwriter_certification_observability.sql"),"utf8").toLowerCase();describe("certification observability",()=>{it("records duration and cost evidence",()=>{expect(sql).toContain("duration_ms");expect(sql).toContain("cost_microunits")});it("limits status to evidence classification",()=>expect(sql).toContain("status in('failed','release_candidate')"));it("states candidacy is not activation",()=>expect(sql).toContain("never means production active"));it("does not grant writes to ordinary users",()=>expect(sql).not.toMatch(/grant (insert|update|delete|all).*authenticated/))});
