import {execFileSync} from "node:child_process";import {readFileSync} from "node:fs";import {describe,expect,it} from "vitest";
const ledger=JSON.parse(readFileSync("release/capability-ledger.json","utf8"));
describe("release contract",()=>{it("remains pre-release and inactive by default",()=>{expect(ledger.classification).toBe("PRE_RELEASE");expect(ledger.capabilities.every((c:Record<string,unknown>)=>c.active===false&&c.certified===false)).toBe(true)});it("passes the fail-closed verifier",()=>{expect(execFileSync(process.execPath,["scripts/verify-release-contract.mjs"],{encoding:"utf8"})).toContain("Release contract valid")});it("keeps distinct implementation and activation states",()=>{for(const capability of ledger.capabilities)expect(Object.keys(capability)).toEqual(expect.arrayContaining(["implemented","deployed","certified","entitled","active"]))})});
